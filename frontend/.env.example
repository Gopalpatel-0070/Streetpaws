# Frontend environment variables for Vite
# Copy to `frontend/.env` or set in your environment during development.
# Vite exposes env vars that start with VITE_ to client code.

# Base URL for backend API (used only if not using proxy)
# Example: http://localhost:4000
VITE_BACKEND_URL=http://localhost:4000

# Any other client-side flags (do NOT put secrets here)
VITE_APP_NAME=StreetPaws
